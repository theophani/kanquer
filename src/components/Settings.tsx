import ThemeToggle from "./ThemeToggle";

export default function Settings() {
  return (
    <div className="settings">
      <span className="settings-label">⚙︎ Preferences</span>
      <ThemeToggle />
    </div>
  )
}
